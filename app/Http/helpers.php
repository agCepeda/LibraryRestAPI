<?php

if (! function_exists("generateRandomToken")) {

	/**
	 * Generate random token with the length specified
	 * by $length parameter
	 * 
	 * @param $length int
	 * @return string
	 */
	function generateRandomToken($length = 10)
	{
		return 0;
	}
}